pipeline {
	agent none
  stages {
  	stage('build docker images') {
    	agent {
      	any {
        }
      }
      steps {
        checkout scm
      	sh '''
        pwd
        cd dispatch
        docker build -t hpjrvs/dispatch:latest .
        docker push hpjrvs/dispatch'''
      }
    }
  }
}