pipeline {
	agent none
  stages {
  	stage('build docker images') {
    	agent {
      	any {
        }
      }
      steps {
        checkout scm
      	sh 'pwd'
        sh 'cd dispatch
        docker build -t hpjrvs/dispatch:latest .'
      }
    }
  }
}