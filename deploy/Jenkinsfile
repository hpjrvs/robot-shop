pipeline {
	agent none
  stages {
  	stage('build docker images') {
    	agent {
      	any {
        	image 'maven:3.5.0'
        }
      }
      steps {
        checkout scm
      	sh 'mvn clean install'
      }
    }
  }
}