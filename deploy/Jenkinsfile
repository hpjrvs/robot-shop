pipeline {
	agent none
  stages {
  	stage('build and push dispatch') {
    	agent {
      	any {
            label 'labelName'
            checkout scm
            
        }
      }
      steps {
        sh '''
        pwd
        cd dispatch
        docker build -t hpjrvs/dispatch .
        docker push hpjrvs/dispatch'''
        
      }
    }
  }
}