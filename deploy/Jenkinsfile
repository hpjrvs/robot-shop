pipeline {
    agent { any { image 'maven:3.9.6-eclipse-temurin-17-alpine' } 
            checkout scm
            }
    stages {
        stage('build dispatch') {
            steps {
                sh 'mvn --version'
            }
        }
    }
}