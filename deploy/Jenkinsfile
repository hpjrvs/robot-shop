pipeline {
	agent none
  stages {
  	stage('build docker images') {
    	agent {
      	docker {
        	image 'maven:3.5.0'
        }
      }
      steps {
        checkout scm
      	sh 'mvn clean install'
      }
    }
  }
}